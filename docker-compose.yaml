version: '3.9'

services:
  front:
    build: 
      context: .
      dockerfile: Dockerfile
    image: front
    container_name: front
    ports:
      - "127.0.0.1:8080:3000" 
      # - "3000:3000"
      # 1-прод, 2-песочница 
  
  # nginx:
  #   image: nginx
  #   container_name: nginx
  #   restart: always
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./etc/nginx/nginx.conf:/etc/nginx/conf.d:ro
  #     - /etc/letsencrypt:/etc/letsencrypt
  #     - ./etc/overschool.by:/etc/nginx/sites-enabled

  # certbot:
  #   image: certbot/certbot:latest
  #   container_name: certbot
  #   volumes:
  #     -./certbot/www/:/var/www/certbot/:rw
  #     -./certbot/conf/:/etc/letsencrypt/:rw
