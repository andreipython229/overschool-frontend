import { employeeModalSvg } from '../constants/svgIcon'
import { pathT } from '../../../../types/commonComponentsTypes'

export const modalIconPath: pathT[] = [{ d: employeeModalSvg, fill: '#E0DCED', strokeWidth: '2', strokeLinecap: 'round', strokeLinejoin: 'round' }]
