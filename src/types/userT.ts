type userInitialT = {
    email:string
    first_name: string
    last_name: string
}

export type  userT = {
    user: userInitialT
    phone_number: string
    avatar: string
    city: string
    description: string
    sex: string
}